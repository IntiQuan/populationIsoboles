#' @import ggplot2
NULL

# "Global" variables for CRAN check
..level.. <- .I <- .N <- .SD <- Drug <- N <- closetopath <- cross <- distToNext <- evaluated <-
evaluatedCum <- gridrowid <- iteration <- itermax <- k <- objvaluei <- patterns <-
percentages <- popk <- value <- variable <- x <- xp <- y <- yp <- NULL

# ###############################################################################-
# # .onAttach ----
# ###############################################################################-
# .onAttach <- function(libname, pkgname) {  }
